# ── Corriendo en local (sin Docker) ──────────────────────────
# DATABASE_URL=postgresql://postgres:tu_contraseña@localhost:5432/notesdb

# ── Corriendo DENTRO de Docker (dev-start.ps1) ───────────────
# host.docker.internal resuelve al localhost de tu maquina Windows
# DATABASE_URL=postgresql://postgres:tu_contraseña@host.docker.internal:5432/notesdb
