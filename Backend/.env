# ── Base de datos ─────────────────────────────────────────────
# Usar esta línea si el backend corre en local (fuera de Docker):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/notesdb

# Usar esta línea si el backend corre DENTRO de un contenedor Docker:
# DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:5432/notesdb

# ── Servidor Flask ────────────────────────────────────────────
DEBUG=true
HOST=0.0.0.0
PORT=5000
